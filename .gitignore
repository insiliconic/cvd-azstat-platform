# Secrets: SMTP/API keys come only from GitHub Secrets / environment variables
.env
.env.*
*.key
secrets.*

# compare_data.py outputs (used only inside the workflow run)
changes.json
changes.md

# Python
__pycache__/
*.pyc
.venv/
venv/
